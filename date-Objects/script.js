// Date arguments(Year, Month, Day, Hour, Minute, Second, Ms)
let date = new Date();

console.log(date.toDateString()); // Outputs date in readable format
console.log(date.toTimeString()); // Outputs time in readable format
console.log(date.toISOString());  // Outputs date in ISO 8601 format

